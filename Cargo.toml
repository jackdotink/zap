[package]
name = "zap"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
lu = "0.4"
uuid = { version = "1.17", features = ["v4"] }

[build-dependencies]
cmake = "0.1"
