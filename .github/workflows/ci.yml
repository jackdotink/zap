name: CI

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
        - name: "Windows x86_64"
          os: windows-latest
          target: x86_64-pc-windows-msvc
          artifact: zap-ci-windows-x86_64
        
        - name: "Linux x86_64"
          os: ubuntu-latest
          target: x86_64-unknown-linux-gnu
          artifact: zap-ci-linux-x86_64
        
        - name: "MacOS x86_64"
          os: macos-latest
          target: x86_64-apple-darwin
          artifact: zap-ci-macos-x86_64
        
        - name: "MacOS aarch64"
          os: macos-latest
          target: aarch64-apple-darwin
          artifact: zap-ci-macos-aarch64

    steps:
    - uses: actions/checkout@v4
    - uses: dtolnay/rust-toolchain@stable
    - uses: swatinem/rust-cache@v2

    - name: Build
      run: cargo build --locked --verbose --release --target ${{ matrix.target }}

    - name: Make Release
      run: |
        mkdir release
        if [ "${{ matrix.os }}" == "windows-latest" ]; then
          cp target/${{ matrix.target }}/release/zap.exe release/zap.exe
        else
          cp target/${{ matrix.target }}/release/zap release/zap
        fi
        zip -r release.zip release
    
    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.artifact }}
        path: release.zip
  
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - uses: dtolnay/rust-toolchain@stable
      with:
        components: rustfmt, clippy
    - uses: swatinem/rust-cache@v2

    - name: Rustfmt
      run: cargo fmt -- --check

    - name: Clippy
      run: cargo clippy